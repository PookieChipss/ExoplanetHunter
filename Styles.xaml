<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ============ COLOR PALETTE ============ -->
    <Color x:Key="BgDeepColor">#0A0E17</Color>
    <Color x:Key="BgPanelColor">#131A2A</Color>
    <Color x:Key="BgPanelLightColor">#1A2338</Color>
    <Color x:Key="GridLineColor">#24304A</Color>
    <Color x:Key="TextPrimaryColor">#E8ECF4</Color>
    <Color x:Key="TextSecondaryColor">#7C8AA8</Color>
    <Color x:Key="SignalAmberColor">#FF9F5B</Color>
    <Color x:Key="ConfirmCyanColor">#5EEAD4</Color>
    <Color x:Key="DangerRedColor">#FF6B6B</Color>

    <SolidColorBrush x:Key="BgDeepBrush" Color="{StaticResource BgDeepColor}"/>
    <SolidColorBrush x:Key="BgPanelBrush" Color="{StaticResource BgPanelColor}"/>
    <SolidColorBrush x:Key="BgPanelLightBrush" Color="{StaticResource BgPanelLightColor}"/>
    <SolidColorBrush x:Key="GridLineBrush" Color="{StaticResource GridLineColor}"/>
    <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource TextPrimaryColor}"/>
    <SolidColorBrush x:Key="TextSecondaryBrush" Color="{StaticResource TextSecondaryColor}"/>
    <SolidColorBrush x:Key="SignalAmberBrush" Color="{StaticResource SignalAmberColor}"/>
    <SolidColorBrush x:Key="ConfirmCyanBrush" Color="{StaticResource ConfirmCyanColor}"/>
    <SolidColorBrush x:Key="DangerRedBrush" Color="{StaticResource DangerRedColor}"/>

    <!-- ============ TYPOGRAPHY ============ -->
    <FontFamily x:Key="DisplayFont">Bahnschrift SemiBold</FontFamily>
    <FontFamily x:Key="BodyFont">Segoe UI</FontFamily>
    <FontFamily x:Key="DataFont">Cascadia Mono</FontFamily>

    <!-- ============ BASE TEXT STYLES ============ -->
    <Style x:Key="ConsoleTitle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource DisplayFont}"/>
        <Setter Property="FontSize" Value="22"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
    </Style>

    <Style x:Key="SectionLabel" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource DisplayFont}"/>
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
    </Style>

    <Style x:Key="DataValue" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource DataFont}"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
    </Style>

    <Style x:Key="DataValueAccent" TargetType="TextBlock" BasedOn="{StaticResource DataValue}">
        <Setter Property="Foreground" Value="{StaticResource SignalAmberBrush}"/>
        <Setter Property="FontSize" Value="15"/>
    </Style>

    <Style x:Key="BodyText" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource BodyFont}"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
    </Style>

    <!-- ============ CONSOLE BUTTON ============ -->
    <Style x:Key="ConsoleButton" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource BgPanelLightBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="FontFamily" Value="{StaticResource DisplayFont}"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Padding" Value="16,12"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="BorderBrush" Value="{StaticResource GridLineBrush}"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource SignalAmberBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource GridLineBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource BgPanelBrush}"/>
                            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                            <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource GridLineBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Primary action variant — amber border always on, used for the main "Run" action -->
    <Style x:Key="ConsoleButtonPrimary" TargetType="Button" BasedOn="{StaticResource ConsoleButton}">
        <Setter Property="BorderBrush" Value="{StaticResource SignalAmberBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource SignalAmberBrush}"/>
    </Style>

    <!-- ============ ROW LIST ITEM ============ -->
    <Style x:Key="ConsoleListBoxItem" TargetType="ListBoxItem">
        <Setter Property="Padding" Value="10,8"/>
        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
        <Setter Property="FontFamily" Value="{StaticResource DataFont}"/>
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Border x:Name="Border" Background="Transparent" BorderThickness="0,0,0,1" BorderBrush="{StaticResource GridLineBrush}">
                        <ContentPresenter Margin="{TemplateBinding Padding}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource BgPanelLightBrush}"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource BgPanelLightBrush}"/>
                            <Setter Property="Foreground" Value="{StaticResource SignalAmberBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ConsoleListBox" TargetType="ListBox">
        <Setter Property="Background" Value="{StaticResource BgPanelBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="BorderBrush" Value="{StaticResource GridLineBrush}"/>
        <Setter Property="ItemContainerStyle" Value="{StaticResource ConsoleListBoxItem}"/>
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
    </Style>

</ResourceDictionary>